<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav"
    app:startDestination="@id/studentsListFragment">

    <fragment
        android:id="@+id/studentsListFragment"
        android:name="com.example.lista7.fragment.StudentsListFragment"
        android:label="StudentsListFragment" >
        <action
            android:id="@+id/action_studentsListFragment_to_studentInfoFragment"
            app:destination="@id/studentInfoFragment" />
    </fragment>
    <fragment
        android:id="@+id/studentInfoFragment"
        android:name="com.example.lista7.fragment.StudentInfoFragment"
        android:label="StudentInfoFragment" >
        <action
            android:id="@+id/action_studentInfoFragment_to_studentsListFragment"
            app:destination="@id/studentsListFragment" />
    </fragment>
</navigation>